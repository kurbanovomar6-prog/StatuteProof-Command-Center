# StatuteProof Bug And Logic Error Register

Date: 2026-06-14

This register captures bugs, logic errors, truth-model conflicts, and demo/commercial readiness issues found during the mega audit. "Fixed in this run" is updated only after a concrete file change is made and validated.

| ID | Severity | Area | File | Problem | Evidence | Recommended fix | Safe to fix now | Fixed in this run |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | P0 | Source readiness | `product/regradar/sources.json`, `tools/validate_parser_quality.py`, frontend source tables, docs | Canonical source-readiness story conflicted: current business instruction said 13 enabled / 10 confirmed / 3 remediation, while committed registry/validator/frontend/docs said 13 enabled / 9 readiness-supported / 4 remediation. | `sources.json` has 13 enabled UAE sources, 9 `active`, 4 `remediation`; `docs/source-readiness-truth-reconciliation-report.md` resolves the safe truth to 13/9/4. | Keep 13 enabled / 9 readiness-supported / 4 remediation until DIFC Laws registry hold is explicitly released. | Yes | Yes |
| BUG-002 | P0 | DFSA source modeling | `product/regradar/sources.json`, `docs/dfsa-selector-investigation-report.md`, `docs/dfsa-source-model-decision.md` | Current DFSA URLs are not valid ready sources; configured URLs produce 404/nav-shell or fail strict remediation-exit gates. | DFSA selector investigation says `rules-and-standards` and `notices-public-registers` fail; `docs/dfsa-source-model-decision.md` defines separate rulebook, enforcement, and AML/MLRO source IDs. | After product-owner approval, migrate/add DFSA source IDs and run no-save plus saved baseline checks. | Decision yes; registry no | Partial |
| BUG-003 | P0 | Demo/evidence | `docs/samples/first-proof-backed-sample-brief.md`, `docs/first-proof-backed-sample-brief-report.md` | Customer demo could rely on sample/demo data unless a real proof-backed artifact was deliberately prepared. | Proof-backed sample brief now references real VARA proof, normalized text, and diff artifacts from `AE-20260612T125401Z-c427013a`. | Convert sample into reviewed alert/weekly-brief preview in a follow-up; do not deliver externally. | Yes | Yes |
| BUG-004 | P0 | Auth/browser | `product/regradar/app/api.py`, `product/regradar/tests/test_auth_plan_contracts.py` | API always set `Secure` session cookies, but local Vite dev proxy is HTTP. Local browser login/register could fail because secure cookies are not persisted over HTTP. | `_session_cookie_secure_for_host()` now keeps production secure by default while allowing localhost cookies over HTTP; focused tests pass. | Run browser smoke with API + Vite before customer demo. | Yes | Yes |
| BUG-005 | P0 | Billing/plan state | `product/regradar/app/plan.py`, `BillingPage.jsx`, `SourceLabPage.jsx`, `product/regradar/tests/test_auth_plan_contracts.py` | Selecting a paid plan recorded intent but `plan.py` returned `status: active` for non-free plans, stronger than manual activation reality. | Paid plan requests now return `pending_manual_activation`, active plan/capabilities remain Source Readiness Review, and Source Lab uses active capabilities for custom-source gating. | Add founder/admin manual activation workflow before first paid pilot. | Yes | Yes |
| BUG-006 | P1 | README/project context | `README.md` | README said live pipeline code is at `regradar/` separate location and this workspace does not contain pipeline code, which was stale and contradicted current project structure. | Current code lives in `product/regradar/`; root context files say product code is in `product/`. | README now points contributors to `product/regradar/`. | Yes | Yes |
| BUG-007 | P1 | Evidence page documentation | `product/regradar/web/src/components/app/EvidencePage.jsx` | File header says no `/api/evidence` endpoint exists, but backend now has `GET /api/evidence`. | `api.py` exposes `_handle_evidence_list`; component fetches `/api/evidence?market=AE`. | Update comment to say API exists but page falls back to sample data when unavailable. Held because validation found unrelated weekly-brief failures and this is a code-file comment. | Yes | No |
| BUG-008 | P1 | Source Lab UX duplication | `SourcesPage.jsx`, `SourceLabPage.jsx` | Sources page contains an older custom-source modal with fewer controls than Source Lab, creating two divergent custom source flows. | `SourcesPage.jsx` has modal, while `/app/source-lab` has advanced options, plan gating, baseline/evidence metrics. | Route "Add custom source" to Source Lab by default or make the modal a wrapper around the same contract. | Yes, frontend-only | No |
| BUG-009 | P1 | Frontend/API source status | `SourcesPage.jsx`, `DashboardHome.jsx`, `api.js` | Source page primarily uses mock data and does not consume `/api/sources/status`; dashboard does, but still has static summary constants. | `SourcesPage.jsx` imports `MOCK_SOURCES`; `DashboardHome.jsx` has static `SOURCE_READINESS_SUMMARY`. | Add API-backed source table mode and fallback clearly labeled as sample/static. | Medium | No |
| BUG-010 | P1 | Validator truth coupling | `tools/validate_parser_quality.py` | Parser quality validator hardcodes that 10/3 source readiness count is stale. This is currently consistent with committed registry but conflicts with the latest business instruction. | Lines checking `"13 enabled"` with `"10 confirmed"` or `"3 under extraction remediation"` fail. | Once source truth is decided, make validator derive expected counts from `sources.json` or a canonical summary file. | No until BUG-001 resolved | No |
| BUG-011 | P1 | Stale project docs | `docs/statuteproof-next-implementation-task.md`, `docs/statuteproof-website-gap-audit.md`, older specs | Several older docs describe outdated routes/modules or claim historical gaps that are now closed. | Examples: old API router path `app/api/v1/router.py`; website gap audit says register not visible; dashboard spec shows DFSA GOOD. | Mark historical docs as archived or add "superseded" headers with latest report links. | Yes, docs-only | No |
| BUG-012 | P1 | Source-specific evidence | `source_runs.py`, `proof.py`, `scraper.py` | Playwright-rendered sources do not produce rendered HTML/screenshot evidence artifacts in the main proof path. | Proof captures text snapshots and metadata, but not browser screenshot/rendered DOM. | Add optional rendered HTML/screenshot paths for Playwright sources. | Medium, separate parser task | No |
| BUG-013 | P1 | PDF quality | `providers/pdf_extraction.py`, `source_intake.py` | PDF cascade extracts text but does not explicitly detect scanned/OCR-needed PDFs beyond shallow/failed text. | Providers return text/page count but no OCR-needed flag. | Add scanned PDF/OCR-needed detection and remediation hints. | Medium, parser task | No |
| BUG-014 | P1 | Registration legal acknowledgement | `RegisterPage.jsx`, `auth.py`, `db.py` | Registration has legal-safe copy but no persisted acknowledgement/version/timestamp field. | Auth schema has user/profile fields but no legal acknowledgement column. | Add required checkbox, store acknowledgement version/timestamp, and test. | Medium | No |
| BUG-015 | P1 | Demo/sample fallback | `EvidencePage.jsx`, `AIBriefPage.jsx`, `AlertsPage.jsx`, `ReportsPage.jsx` | API failures can silently leave users in sample mode, which is useful but can hide backend issues during QA. | `EvidencePage.jsx` catches API failure silently and shows sample mode. | Add explicit QA-visible API state and demo/live toggle banner. | Yes, frontend-only | No |
| BUG-016 | P1 | Delivery safety | `IntegrationsPage.jsx`, `user_delivery.py`, `api.py` | Sample brief/test delivery exists and can send if configured; demo scripts must avoid accidental customer delivery. | `/api/delivery/test-brief`, `/api/telegram/test`, and preview alert delivery exist. | Add demo-mode warning and require explicit confirmation for any send/test action in customer demo. | Medium | No |
| BUG-017 | P2 | Internal naming | `source_certification.py`, `source_intake.py`, `api.py`, `SourceLabPage.jsx` | Internal "certification" vocabulary remains in code/API fields. UI maps most labels safely, but future customer contracts could leak stronger wording. | API returns `certification_status` and `certification`; Source Lab uses variable names but safer display labels. | Long-term rename API contract to activation readiness/source readiness while keeping backward compatibility. | No | No |
| BUG-018 | P2 | Source count constants | `DashboardPreview.jsx`, `DashboardHome.jsx`, `Pricing.jsx`, `PricingPage.jsx`, `BillingPage.jsx`, `SourceCoverageTable.jsx`, `appMockData.js` | Source counts are duplicated across many frontend files. Manual duplication created the current truth-fragmentation risk. | Static `SOURCE_PACK_SUMMARY`, `SOURCE_READINESS_SUMMARY`, and repeated text strings. | Add one frontend data module generated from or validated against canonical registry summary. | Yes after BUG-001 | No |
| BUG-019 | P2 | Parser benchmark depth | `tests/test_parser_benchmark_suite.py`, docs | Parser score is improved but not 10/10; benchmark corpus is not broad enough to prove robustness across regulator archetypes. | Parser final report scores parser 7.9/10. | Create benchmark fixtures for HTML, JS, PDF, nav-shell, WAF-like, table-heavy, multilingual, and shallow pages. | Yes, separate task | No |
| BUG-020 | P2 | Deployment docs | `README.md`, `docs/actual-hosting-location-audit.md`, deployment docs | Hosting state is documented but not freshly verified. README still has stale location wording. | Latest mega audit did not deploy or verify live infrastructure. | Run deployment readiness audit before any production action. | Docs yes, infra no | No |
| BUG-021 | P1 | Weekly brief validation | `product/regradar/tests/test_weekly_brief.py` | Weekly brief tests expected an older short disclaimer and older no-change wording, while rendered output now uses stronger legal-safe disclaimer and more precise monitoring language. | `python3 -m pytest product/regradar/tests/test_weekly_brief.py -q` previously failed 2 expectations. | Update tests to assert the current safer disclaimer and current no-detected-change wording. | Yes | Yes |

## Counts

- P0 blockers found: 5
- P1 bugs/improvements found: 12
- P2 bugs/improvements found: 4
- Bugs fixed in this P0 sprint so far: 6, including 4 full P0 fixes, 1 partial DFSA model decision, and 1 P1 validation fix.

## Highest-Leverage Fix Order

1. Run approved DFSA source-model migration/no-save/baseline task.
2. Run browser auth smoke with API + Vite before customer demo.
3. Convert the proof-backed VARA sample into a reviewed, non-delivered weekly brief preview.
4. Build founder/admin manual activation workflow.
5. Generate one canonical source-readiness summary consumed by validators and frontend.
